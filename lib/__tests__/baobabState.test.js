const assert = require('assert');
const baobabState = require('../index.js');

describe('baobabState', () => {
  it('has a test', () => {
    assert.deepEqual(baobabState, {}, 'baobabState is not null');
  });
});
