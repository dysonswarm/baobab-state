const assert = require('assert');
const baobabState = require('../index.js');

describe('baobabState', () => {
  it('has a test', () => {
    assert(false, 'baobabState should have a test');
  });
});
